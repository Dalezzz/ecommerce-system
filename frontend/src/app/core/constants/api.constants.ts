// En producción, si el frontend y backend están en el mismo dominio, se puede usar una ruta relativa
// Si están en dominios distintos, se debe usar la URL completa (ej. https://api.mi-tienda.com/api/v1)
export const API_BASE_URL = '/api/v1';

export const AUTH_TOKEN_KEY = 'ecommerce_auth_token';
